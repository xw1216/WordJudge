# Default Configuration File Used By Hydra

defaults:
  - _self_
  - dataset: WordJudge
  - model: BrainGNN
  - train: Train
  - log: Log

project: 'WordJudge_Atlas_lim'
entity: 'outspace'
group: 'Atlas_lim_RRC_wRRC'

cuda: True

hydra:
  run:
    dir: ./log/hydra/${now:%Y-%m-%d}/${now:%H-%M-%S}
