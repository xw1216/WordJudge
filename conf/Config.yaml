# Default Configuration File Used By Hydra

defaults:
  - _self_
  - dataset: WordJudge
  - model: BrainGNN
  - train: Train
  - log: Log

project: 'WordJudge'
entity: 'outspace'
group: 'initial'

cuda: True
