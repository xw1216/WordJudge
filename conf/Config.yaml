# Default Configuration File Used By Hydra

defaults:
  - _self_
  - dataset: WordJudge
  - model: BrainGNN
  - train: Train
  - log: Log

project: 'WordJudge_test'
entity: 'outspace'
group: 'RRC_wRRC'

cuda: True

hydra:
  run:
    dir: ./log/hydra/${now:%Y-%m-%d}/${now:%H-%M-%S}
